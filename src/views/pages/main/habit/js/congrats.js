const done = document.querySelectorAll(".done")
const checkok = document.querySelectorAll(".checkok")

done.forEach((done) =>
  done.addEventListener("click", () => {
    // alert("TERAFA CONCLUIDA COM SUCESSO. MUITOS PARABÉNS!!")
  })
)
